# Write your MySQL query statement below
select b.name Department, a.name Employee , a.salary Salary from Employee a left join Department b on a.departmentId=b.id where a.salary=(select max(salary) from Employee where departmentId=a.departmentId);